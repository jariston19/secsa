# Copy to .env before running docker compose up.
# Generate a secret: openssl rand -hex 32  (or: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=
SEED_ON_START=false
